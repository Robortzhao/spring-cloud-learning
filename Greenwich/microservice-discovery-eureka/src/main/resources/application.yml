server:
  port: 8761            #指定该Eureka实例的端口
eureka:
  client:
    register-with-eureka: false #是否要注册到其他EurekaServer实例
    fetch-registry: false       #是否要从其他EurekaServer实例获取数据
    service-url:
      defaultZone: http://localhost:8761/eureka/
  server:
    enable-self-preservation: false
